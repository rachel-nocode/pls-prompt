# PlsPrompt To-Do

- [ ] Set up `www.plsprompt.com` separately: add the hostname to Sites, configure the required Porkbun DNS records, verify HTTPS, and configure and verify a redirect to `https://plsprompt.com`.

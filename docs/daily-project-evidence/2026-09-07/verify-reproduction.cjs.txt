const {chromium}=require('/Users/witchaudio/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const http=require('http'),fs=require('fs'),path=require('path'),assert=require('assert');
const root=__dirname, report={model:'GPT-6 Astra (High), inherited parent configuration',checks:[],errors:[],requests:[],repairs:['Test harness scrolls editor into view before coordinate-based pointer interactions; first run clicked outside the scrolled viewport.'],limitations:['Independent agent reproduction, not independent human-user coverage.','Clipboard success and denial simulated because opaque-origin clipboard access is browser-policy dependent.','Touch exercised through Chromium CDP emulation; no physical touchscreen.','Only Chromium tested.']};
const check=(name,fn)=>Promise.resolve().then(fn).then(()=>report.checks.push({name,result:'PASS'}));
const server=http.createServer((req,res)=>{res.setHeader('Content-Type','text/html');if(req.url==='/host'){res.end('<!doctype html><style>html,body{margin:0}iframe{border:0;width:100%;height:4000px;display:block}</style><script>window.ready=[];addEventListener("message",e=>ready.push({origin:e.origin,data:e.data}))</script><iframe title="Glyph Loom" sandbox="allow-scripts" src="/glyph-loom.html"></iframe>')}else if(req.url==='/glyph-loom.html')res.end(fs.readFileSync(path.join(root,'glyph-loom.html')));else{res.statusCode=404;res.end('')}});
(async()=>{await new Promise(r=>server.listen(0,'127.0.0.1',r));const browser=await chromium.launch({headless:true});const page=await browser.newPage({viewport:{width:1440,height:1100},hasTouch:true});page.on('pageerror',e=>report.errors.push(e.message));page.on('request',r=>report.requests.push(r.url()));await page.goto(`http://127.0.0.1:${server.address().port}/host`);const f=page.frames()[1],click=id=>f.locator('#'+id).click(),ascii=()=>f.locator('#ascii').inputValue(),count=async()=>((await ascii()).match(/#/g)||[]).length,reset=()=>click('reset'),clear=()=>click('clear'),select=(id,v)=>f.locator('#'+id).selectOption(v),stamp=async(r,c)=>{await f.locator('#row').fill(String(r));await f.locator('#col').fill(String(c));await click('stamp')};const expected=name=>Array.from({length:16},(_,y)=>Array.from({length:16},(_,x)=>{let d=Math.abs(x-7.5)+Math.abs(y-7.5),r=(x-7.5)**2+(y-7.5)**2;return (name==='diamond'?d>=5&&d<=7:name==='steps'?(Math.floor(x/2)+Math.floor(y/2))%4===0:r>=25&&r<=42)?'#':'.'}).join('')).join('\n');
try{
await check('Opaque-origin readiness, initial state, exact ASCII shape',async()=>{assert((await page.evaluate(()=>ready)).some(r=>r.origin==='null'&&r.data.type==='pls-demo-ready'));assert.equal(await ascii(),expected('diamond'));assert.match(await ascii(),/^[#.]{16}(\n[#.]{16}){15}$/);assert(await f.locator('#undo').isDisabled());assert(await f.locator('#redo').isDisabled())});await page.screenshot({path:path.join(root,'initial.png'),fullPage:true});
await check('All deterministic presets, 256 cells and repeat pixels',async()=>{for(const name of ['diamond','steps','orbit']){await f.locator(`[data-preset=${name}]`).click();assert.equal(await ascii(),expected(name));assert.equal(await f.evaluate(()=>tile.length),256);const valid=await f.evaluate(()=>{const rgb=[168,235,203],r=document.getElementById('repeat').getContext('2d');for(let y=0;y<48;y++)for(let x=0;x<48;x++){let v=tile[(y%16)*16+x%16],p=r.getImageData(x*16+8,y*16+8,1,1).data;if(![0,1,2].every(i=>p[i]===(v?rgb[i]:17)))return false}return true});assert(valid)}});
await check('Both axes, no-op stamp, one-step Undo/Redo, erase with None',async()=>{await reset();await clear();await stamp(2,3);const a=(await ascii()).split('\n');assert.equal(await count(),4);for(const [r,c]of [[2,3],[2,14],[15,3],[15,14]])assert.equal(a[r-1][c-1],'#');await stamp(2,3);await click('undo');assert.equal(await count(),0);await click('redo');assert.equal(await count(),4);await select('mirror','none');await click('erase');await stamp(2,3);assert.equal(await count(),3);await click('undo');assert.equal(await count(),4);assert.equal(await f.locator('#mirror').inputValue(),'none');assert.equal(await f.locator('#erase').getAttribute('aria-pressed'),'true')});
await check('Left/right mirror and tile-only history',async()=>{await clear();await click('ink');await select('mirror','lr');await stamp(4,5);assert.equal(await count(),2);const a=(await ascii()).split('\n');assert.equal(a[3][4],'#');assert.equal(a[3][11],'#')});
await check('Sparse pointer interpolation and single undo',async()=>{await reset();await clear();await select('mirror','none');await f.locator('#editor').scrollIntoViewIfNeeded();const b=await f.locator('#editor').boundingBox();await page.mouse.move(b.x+b.width*1.5/16,b.y+b.height*2.5/16);await page.mouse.down();await page.mouse.move(b.x+b.width*12.5/16,b.y+b.height*2.5/16);await page.mouse.up();assert.equal(await count(),12);await click('undo');assert.equal(await count(),0);await click('redo');assert.equal(await count(),12)});
await check('Pointer exit clamps and keeps completed marks',async()=>{await clear();await f.locator('#editor').scrollIntoViewIfNeeded();const b=await f.locator('#editor').boundingBox();await page.mouse.move(b.x+b.width*10.5/16,b.y+b.height*.5/16);await page.mouse.down();await page.mouse.move(b.x+b.width+50,b.y+b.height*.5/16);await page.mouse.up();assert.equal(await count(),6);await click('undo');assert.equal(await count(),0)});
await check('Lost pointer capture finalizes once',async()=>{await clear();await f.locator('#editor').scrollIntoViewIfNeeded();const b=await f.locator('#editor').boundingBox();await page.mouse.move(b.x+b.width*.5/16,b.y+b.height*.5/16);await page.mouse.down();await f.evaluate(()=>editor.releasePointerCapture(stroke.id));await page.mouse.up();assert.equal(await count(),1);await click('undo');assert.equal(await count(),0)});
await check('Touch drag and touch cancellation',async()=>{await clear();await f.locator('#editor').scrollIntoViewIfNeeded();const b=await f.locator('#editor').boundingBox(),cdp=await page.context().newCDPSession(page);const p=(x,y)=>({x:b.x+b.width*x/16,y:b.y+b.height*y/16});await cdp.send('Input.dispatchTouchEvent',{type:'touchStart',touchPoints:[p(1.5,4.5)]});await cdp.send('Input.dispatchTouchEvent',{type:'touchMove',touchPoints:[p(7.5,4.5)]});await cdp.send('Input.dispatchTouchEvent',{type:'touchEnd',touchPoints:[]});assert.equal(await count(),7);await click('undo');assert.equal(await count(),0);await cdp.send('Input.dispatchTouchEvent',{type:'touchStart',touchPoints:[p(4.5,4.5)]});await cdp.send('Input.dispatchTouchEvent',{type:'touchCancel',touchPoints:[]});assert.equal(await count(),1);await click('undo');assert.equal(await count(),0);await cdp.detach()});
await check('Diagonal Bresenham and primary-button filtering',async()=>{await clear();await f.locator('#editor').scrollIntoViewIfNeeded();const b=await f.locator('#editor').boundingBox();await page.mouse.move(b.x+b.width*2.5/16,b.y+b.height*2.5/16);await page.mouse.down({button:'right'});await page.mouse.up({button:'right'});assert.equal(await count(),0);await page.mouse.down();await page.mouse.move(b.x+b.width*10.5/16,b.y+b.height*10.5/16);await page.mouse.up();assert.equal(await count(),9);const a=(await ascii()).split('\n');for(let i=2;i<=10;i++)assert.equal(a[i][i],'#');await click('undo');assert.equal(await count(),0);await f.locator('#editor').evaluate(e=>e.dispatchEvent(new PointerEvent('pointerdown',{isPrimary:false,button:0,pointerId:99,bubbles:true})));assert.equal(await count(),0)});
await check('Active stroke is finalized before settings action',async()=>{await clear();await f.locator('#editor').scrollIntoViewIfNeeded();const b=await f.locator('#editor').boundingBox();await page.mouse.move(b.x+b.width*.5/16,b.y+b.height*.5/16);await page.mouse.down();await f.locator('#erase').evaluate(e=>e.click());await page.mouse.up();assert.equal(await count(),1);assert.equal(await f.evaluate(()=>stroke),null);await click('undo');assert.equal(await count(),0);await click('ink')});
await check('Keyboard clamp, Home/End, Space/Enter, focus and inputs',async()=>{await reset();await clear();await select('mirror','none');const ed=f.locator('#editor');await ed.focus();await ed.press('ArrowLeft');await ed.press('ArrowUp');assert.equal(await f.locator('#row').inputValue(),'1');assert.equal(await f.locator('#col').inputValue(),'1');await ed.press('End');await ed.press('ArrowRight');assert.equal(await f.locator('#col').inputValue(),'16');await ed.press('Space');assert.equal(await count(),1);await ed.press('Home');await ed.press('Enter');assert.equal(await count(),2);assert(await ed.evaluate(e=>e===document.activeElement));await ed.press('ArrowDown');assert.equal(await f.locator('#row').inputValue(),'2')});
await check('Reject empty, 0, 17 and fractional coordinates without history',async()=>{let before=await ascii(),n=await f.evaluate(()=>past.length);for(const v of ['','0','17','1.5']){await f.locator('#row').fill(v);await click('stamp');assert.equal(await ascii(),before);assert.equal(await f.evaluate(()=>past.length),n);assert.match(await f.locator('#status').textContent(),/whole/)}await f.locator('#row').fill('1')});
await check('No-op clear/preset/stamp history and redo invalidation',async()=>{await reset();await f.locator('[data-preset=diamond]').click();assert(await f.locator('#undo').isDisabled());await clear();const n=await f.evaluate(()=>past.length);await clear();assert.equal(await f.evaluate(()=>past.length),n);await select('mirror','none');await stamp(1,1);await click('undo');assert(!(await f.locator('#redo').isDisabled()));await stamp(1,2);assert(await f.locator('#redo').isDisabled())});
await check('50-action undo cap',async()=>{await reset();await clear();for(let i=0;i<55;i++)await click('invert');assert.equal(await f.evaluate(()=>past.length),50);for(let i=0;i<50;i++)await click('undo');assert(await f.locator('#undo').isDisabled());assert.equal(await count(),256)});
await check('Double invert identity, palette ASCII invariance, metrics',async()=>{await reset();const before=await ascii();await click('invert');await click('invert');assert.equal(await ascii(),before);await select('palette','#edc583');assert.equal(await ascii(),before);const n=await count();assert.equal(await f.locator('#metrics').textContent(),`${n} inked cells · ${Math.round(n/256*100)}% coverage`)});
await check('Reset restores all settings and empties history',async()=>{await click('erase');await select('mirror','none');await stamp(8,9);await reset();assert.equal(await ascii(),expected('diamond'));assert.equal(await f.locator('#row').inputValue(),'1');assert.equal(await f.locator('#col').inputValue(),'1');assert.equal(await f.locator('#palette').inputValue(),'#a8ebcb');assert.equal(await f.locator('#mirror').inputValue(),'both');assert.equal(await f.locator('#ink').getAttribute('aria-pressed'),'true');assert(await f.locator('#undo').isDisabled());assert(await f.locator('#redo').isDisabled())});
await check('Copy success, fallback success, denial and full selection',async()=>{await f.evaluate(()=>{Object.defineProperty(navigator,'clipboard',{configurable:true,value:{writeText:async()=>{}}})});await click('copy');assert.equal(await f.locator('#status').textContent(),'Copied');await f.evaluate(()=>{Object.defineProperty(navigator,'clipboard',{configurable:true,value:{writeText:async()=>{throw Error('denied')}}});document.execCommand=()=>true});await click('copy');assert.equal(await f.locator('#status').textContent(),'Copied');await f.evaluate(()=>{Object.defineProperty(navigator,'clipboard',{configurable:true,value:undefined});document.execCommand=undefined});await click('copy');assert.match(await f.locator('#status').textContent(),/^Copy unavailable/);await click('select');assert(await f.locator('#ascii').evaluate(e=>e===document.activeElement&&e.selectionStart===0&&e.selectionEnd===e.value.length));await clear();assert.equal(await count(),0)});
await check('Responsive widths and 200% root text: no page overflow, square canvases',async()=>{for(const size of [320,375,768,1024,1440,1600])for(const zoom of [1,2]){await page.setViewportSize({width:size,height:1100});await f.evaluate(z=>document.documentElement.style.fontSize=16*z+'px',zoom);const m=await f.evaluate(()=>({w:innerWidth,sw:document.documentElement.scrollWidth,canvases:[...document.querySelectorAll('canvas')].map(e=>({w:e.getBoundingClientRect().width,h:e.getBoundingClientRect().height})),controls:[...document.querySelectorAll('button,input,select')].map(e=>({w:e.getBoundingClientRect().width,h:e.getBoundingClientRect().height}))}));assert(m.sw<=m.w,`${size}/${zoom}: ${JSON.stringify(m)}`);assert(m.canvases.every(c=>Math.abs(c.w-c.h)<1));assert(m.controls.every(c=>c.h>=44));await page.screenshot({path:path.join(root,`width-${size}-text-${zoom}.png`),fullPage:true})}});
await page.setViewportSize({width:1440,height:1100});await f.evaluate(()=>document.documentElement.style.fontSize='16px');await f.locator('[data-preset=steps]').click();await select('palette','#c7b6ef');await page.screenshot({path:path.join(root,'changed.png'),fullPage:true});
await check('No external requests or script errors',async()=>{assert.equal(report.errors.length,0);assert(report.requests.every(u=>u.startsWith(`http://127.0.0.1:${server.address().port}/`)));assert.equal(report.requests.length,2)});
report.result='PASS';
}catch(e){report.result='FAIL';process.exitCode=1;report.failure=e.stack;console.error(e)}finally{fs.writeFileSync(path.join(root,'evidence.json'),JSON.stringify(report,null,2));console.log(JSON.stringify(report,null,2));await browser.close();server.close()}
})().catch(e=>{console.error(e);process.exitCode=1;server.close()});

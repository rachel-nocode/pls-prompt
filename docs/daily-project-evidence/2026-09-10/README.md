# Revision Lens — September 10, 2026

Distinct daily text comparison workbench. Full 18-concept ledger and all 18 live rows inspected (offsets 0/6/14, final has_more=false), plus nine live gallery links. Baseline clean 733e5a4; exclusive automation lock and no competing repository task/process. Existing owner-only audience revision 1. User-directed active checkout retained; no synced sources edits.

## Build and reproduction

Recipe v1 is a consolidated two-step specification and verification pack, not a one-shot claim or transcript. Downloads contain complete recipe only; standalone source artifacts here are repository-only evidence. Dependency-free app uses exact case-sensitive Word/Line normalization, deterministic LCS, staged edits and reversible report.

Original passed 11 Chromium groups and 14,641 exhaustive three-symbol sequence pairs of length 0–4, compared against independent prefix minimum-edit distance and reconstruction. Coverage includes both normalization modes; empty/case/punctuation/repetition; deterministic tie; 200/201 unit and 6000/6001 UTF-16 boundaries; input atomicity; pending edits/swap/filter/presets/reset; harmless HTML; report parsing/selection; injected copy success/fallback/denial and focus; six widths, 200% text with 200-unit counters, touch/reduced motion and sandbox readiness/storage denial; no external requests or script errors.

Initial test corrected specification arithmetic: sample actually has seven words each side and five unchanged, two removed and two added. Original algorithm was already correct. Spec review identified undersized run/report text and overlapping enlarged three-digit counters; font minima raised, cards wrap with minimum width, affected checks rerun. Spec rereview passed. Final independent quality review passed, additionally checking astral UTF-16 limits, 200/201 lines, atomic report and preset recovery. Real original initial/line screenshots visually inspected.

Fresh-context GPT-6 Astra High independently built from only two prompt files in an empty temporary directory without original source or screenshots. 21 groups and 961 binary-sequence oracle pairs passed. First run had one arithmetic assertion failure; corrected recipe expectation, no reproduction algorithm or HTML repair. Both first-run and passing evidence retained. No identical-pixel claim. Reproduction server stopped.

## Host checks

Build, typecheck, lint and 72 repository regressions passed. All four prior daily JSON payloads structurally unchanged. No schema, authentication, library, analytics or audience changes. Initial local host check waited for missing catalog because Wrangler used an empty default state directory; restarted with explicit existing .wrangler/state (no code repair). Eight local production host groups passed. Live results recorded separately. Native browser recipe save checked locally; live authenticated attachment compared byte-for-byte. Download analytics check uses local HEAD +0 / GET +1 / bot and errors +0. Existing authentication and library preservation covered by regression suite; no production private-save mutation claimed.

## Limits

Chromium/Chrome only; touch emulated and clipboard branches injected. No Safari, physical-device, screen-reader, independent-human or full accessibility audit. Input caps deliberately disclosed as UTF-16 and normalized units. Viewport tests are overflow/control checks, not proof of every possible content layout. Background automation skips user-facing browser handoff. No new services, credentials, paid dependencies or payments.

## Publication

Sites version 12 succeeded at 2026-09-10T16:58:32.529882+00:00 from exact validated/pushed source 5186312bd63b3fa1e7ab3630febf116dde5d1d3f. Deployment appgdep_6aa2e1aa8e8881918c2bb000790272d1. Native success URL https://plsprompt.witch-audio.chatgpt.site; verified custom project URL https://plsprompt.com/prompts/revision-lens. All six live host groups passed; ten gallery demos present, exact demo source/CSP matched, and live authenticated recipe attachment equals native local download byte-for-byte. Existing owner-only audience retained, environment revision 1. Sole September 10 publication. Owned preview servers stopped and lock removed at completion.

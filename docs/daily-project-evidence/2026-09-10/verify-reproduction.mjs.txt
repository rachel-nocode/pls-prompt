import {createRequire} from 'node:module';import fs from 'node:fs';import http from 'node:http';import assert from 'node:assert/strict';
const require=createRequire(import.meta.url);const {chromium}=require('/Users/witchaudio/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const root='/tmp/pls-revision-reproduction',results=[],requests=[],errors=[];let browser;
const html=fs.readFileSync(root+'/revision-lens.html','utf8');const host=`<!doctype html><html><head><meta name="viewport" content="width=device-width,initial-scale=1"><style>html,body{margin:0;width:100%;height:100%}iframe{display:block;width:100%;height:100%;border:0}</style></head><body><iframe sandbox="allow-scripts" src="/revision-lens.html"></iframe><script>window.messages=[];addEventListener('message',e=>messages.push({origin:e.origin,data:e.data}))</script></body></html>`;
const server=http.createServer((req,res)=>{res.setHeader('Content-Type','text/html');res.end(req.url==='/revision-lens.html'?html:host)});await new Promise(r=>server.listen(5181,'127.0.0.1',r));
async function check(name,fn){try{await fn();results.push({name,passed:true})}catch(e){results.push({name,passed:false,error:String(e.stack||e)})}}
try{browser=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true});const page=await browser.newPage({viewport:{width:1440,height:1100}});page.on('request',r=>requests.push(r.url()));page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:5181');const frame=page.frames().find(f=>f.url().endsWith('/revision-lens.html'));await frame.locator('#report').waitFor();
const snap=()=>frame.evaluate(()=>({report:document.getElementById('report').value,counts:document.getElementById('counts').innerText,mode:document.getElementById('committedMode').textContent,pending:!document.getElementById('pending').hidden,runs:document.getElementById('runs').innerText}));
const compare=async(a,b,mode='Word')=>{await frame.locator('#before').fill(a);await frame.locator('#after').fill(b);await frame.locator('#mode').selectOption(mode);await frame.locator('#compare').click();return frame.locator('#report').inputValue()};
const parse=report=>report.split('\n').filter(s=>/^[=+-] /.test(s)).map(s=>[s[0],JSON.parse(s.slice(2))]);const reconstruct=ops=>[ops.filter(x=>x[0]!=='+').map(x=>x[1]),ops.filter(x=>x[0]!=='-').map(x=>x[1])];
function oracle(a,b){const d=Array.from({length:a.length+1},()=>Array(b.length+1).fill(0));for(let i=0;i<=a.length;i++)d[i][0]=i;for(let j=0;j<=b.length;j++)d[0][j]=j;for(let i=1;i<=a.length;i++)for(let j=1;j<=b.length;j++)d[i][j]=a[i-1]===b[j-1]?d[i-1][j-1]:Math.min(d[i-1][j]+1,d[i][j-1]+1);return d[a.length][b.length]}
await check('Initial committed counts and screenshot',async()=>{const s=await snap();for(const x of ['Before total: 7','After total: 7','Unchanged: 5','Removed: 2','Added: 2'])assert(s.report.includes(x));await page.screenshot({path:root+'/initial.png',fullPage:true})});
await check('Null-origin ready and storage denied',async()=>{assert((await page.evaluate(()=>messages)).some(x=>x.origin==='null'&&x.data.type==='pls-demo-ready'));assert(await frame.evaluate(()=>{try{localStorage.setItem('x','x');return false}catch{return true}}));assert.equal(await frame.locator('meta[http-equiv="Content-Security-Policy"]').getAttribute('content'),"default-src 'none'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; img-src data:; connect-src 'none'")});
await check('Packing counts and line screenshot',async()=>{await frame.locator('#packing').click();const r=await frame.locator('#report').inputValue();for(const x of ['Unchanged: 3','Removed: 1','Added: 1'])assert(r.includes(x));await page.screenshot({path:root+'/line.png',fullPage:true})});
await check('Exhaustive independent edit-distance oracle: 961 sequence pairs',async()=>{const seq=[[]];for(let n=1;n<=4;n++)for(let mask=0;mask<2**n;mask++)seq.push(Array.from({length:n},(_,i)=>(mask>>i)&1?'B':'A'));const out=await frame.evaluate(seq=>seq.flatMap(a=>seq.map(b=>({a,b,ops:diff(a,b)}))),seq);assert.equal(out.length,961);for(const {a,b,ops} of out){assert.deepEqual(ops.filter(x=>x[0]!=='add').map(x=>x[1]),a);assert.deepEqual(ops.filter(x=>x[0]!=='remove').map(x=>x[1]),b);assert.equal(ops.filter(x=>x[0]!=='equal').length,oracle(a,b))}});
await check('Deletion-first deterministic tie',async()=>assert.deepEqual(parse(await compare('A B','B A')),[['-','A'],['=','B'],['+','A']]));
await check('Empty, identical, addition, removal, repeated, case and punctuation behavior',async()=>{for(const [a,b] of [['',''],['x x','x x'],['','a b'],['a b',''],['a a b','a b a'],['Case end.','case end!']]){const aa=a?a.split(' '):[],bb=b?b.split(' '):[];const ops=parse(await compare(a,b));assert.deepEqual(reconstruct(ops),[aa,bb]);assert.equal(ops.filter(x=>x[0]!=='=').length,oracle(aa,bb))}await compare('','');assert.equal(await frame.locator('#equality').innerText(),'Both texts are empty.');await compare('same','same');assert.equal(await frame.locator('#equality').innerText(),'No differences.')});
await check('Unicode Word whitespace normalization and equal status',async()=>{assert.deepEqual(parse(await compare(' a\t b\n\u2003c  ','a b c')),[['=','a'],['=','b'],['=','c']]);assert.equal(await frame.locator('#equality').innerText(),'No differences.')});
await check('Line blank/whitespace/terminal, CRLF/lone-CR normalization and report JSON',async()=>{const a='a\r\n\r  \r',b='a\n\n\t\n';const r=await compare(a,b,'Line');assert.deepEqual(reconstruct(parse(r)),[['a','','  ',''],['a','','\t','']]);assert((await snap()).runs.includes('(empty line)'));assert((await snap()).runs.includes('(whitespace line)'));const direct=await frame.evaluate(()=>units('A\r\nB\r','Line'));assert.deepEqual(direct,['A','B','']);assert.deepEqual(reconstruct(parse(await compare('" \\ \t\n','"\n','Line'))),[['" \\ \t',''],['"','']])});
await check('Literal HTML/script-like input is inert',async()=>{const s='<script>window.hacked=1</script> <img src=x onerror=window.hacked=1>';await compare('',s);assert.deepEqual(reconstruct(parse(await frame.locator('#report').inputValue())),[[],s.split(' ')]);assert.equal(await frame.evaluate(()=>window.hacked),undefined);assert.equal(await frame.locator('#runs script, #runs img').count(),0)});
await check('200 units accepted; 201 rejected atomically without truncation',async()=>{const a=Array(200).fill('a').join(' ');await compare(a,a);const prev=await snap();const b=a+' a';await frame.locator('#before').fill(b);await frame.locator('#compare').click();assert.equal(await frame.locator('#before').inputValue(),b);assert.equal(await frame.locator('#before').getAttribute('aria-invalid'),'true');assert.deepEqual({...await snap(),pending:false},prev);await frame.locator('#before').fill('fixed');assert.equal(await frame.locator('#before').getAttribute('aria-invalid'),null)});
await check('6000 UTF-16 units accepted; 6001 rejected atomically; both fields validated',async()=>{const a='😀'.repeat(3000);await compare(a,a);const prev=await snap();await frame.locator('#before').fill(a+'x');await frame.locator('#after').fill(a+'y');await frame.locator('#compare').click();assert.equal((await frame.locator('#before').inputValue()).length,6001);for(const id of ['before','after'])assert.equal(await frame.locator('#'+id).getAttribute('aria-invalid'),'true');assert.deepEqual({...await snap(),pending:false},prev);await frame.locator('#before').fill('a');assert.equal(await frame.locator('#before').getAttribute('aria-invalid'),null);assert.equal(await frame.locator('#after').getAttribute('aria-invalid'),'true')});
await check('Edits and mode stay pending; Swap stages raw text; Compare commits',async()=>{await compare('old a','old b');const prev=await snap();await frame.locator('#before').fill('new\nA');await frame.locator('#after').fill('new\nB');await frame.locator('#mode').selectOption('Line');assert.deepEqual({...await snap(),pending:false},prev);await frame.locator('#swap').click();assert.equal(await frame.locator('#before').inputValue(),'new\nB');assert.equal(await frame.locator('#after').inputValue(),'new\nA');assert.deepEqual({...await snap(),pending:false},prev);await frame.locator('#compare').click();assert.equal((await snap()).pending,false);assert((await snap()).mode.endsWith('Line'))});
await check('Changes only keeps report/counts; persists Compare/Swap/Clear; empty message/context',async()=>{await frame.locator('#packing').click();const prev=await snap();await frame.locator('#filter').check();const filtered=await snap();assert.equal(filtered.report,prev.report);assert.equal(filtered.counts,prev.counts);assert(!filtered.runs.includes('= Unchanged'));await frame.locator('#compare').click();assert(await frame.locator('#filter').isChecked());await frame.locator('#swap').click();assert(await frame.locator('#filter').isChecked());await frame.locator('#clear').click();assert(await frame.locator('#filter').isChecked());assert((await snap()).mode.endsWith('Line'));assert.deepEqual(parse((await snap()).report),[]);assert(await frame.locator('#noChanges').isVisible());assert.equal(await frame.locator('#equality').innerText(),'Both texts are empty.');await compare('same','same','Line');assert(await frame.locator('#noChanges').isVisible());await frame.locator('#filter').uncheck();assert((await snap()).runs.includes('same'))});
await check('Presets and Reset replace pending edits and reset filter; button focus survives',async()=>{for(const id of ['editorial','packing','reset']){await frame.locator('#filter').check();await frame.locator('#before').fill('pending');await frame.locator('#'+id).click();assert(!(await frame.locator('#filter').isChecked()));assert.equal((await snap()).pending,false);assert.equal(await frame.evaluate(()=>document.activeElement.id),id);assert.equal(await frame.locator('#mode').inputValue(),id==='packing'?'Line':'Word')}});
await check('Select report selects all; injected clipboard success, fallback success/failure and focus',async()=>{await frame.locator('#selectReport').click();assert(await frame.evaluate(()=>{const r=document.getElementById('report');return document.activeElement===r&&r.selectionStart===0&&r.selectionEnd===r.value.length}));await frame.evaluate(()=>{Object.defineProperty(navigator,'clipboard',{configurable:true,value:{writeText:async x=>{window.copied=x}}})});await frame.locator('#copy').click();await frame.waitForFunction(()=>document.getElementById('status').textContent==='Copied');assert.equal(await frame.evaluate(()=>window.copied),(await snap()).report);for(const ok of [true,false]){await frame.evaluate(ok=>{Object.defineProperty(navigator,'clipboard',{configurable:true,value:{writeText:async()=>{throw Error('denied')}}});document.execCommand=()=>ok},ok);await frame.locator('#copy').click();await frame.waitForFunction(()=>document.activeElement.id==='copy');assert.equal(await frame.locator('#status').innerText(),ok?'Copied':'Copy unavailable. Use Select report and your browser copy command.')}await frame.evaluate(()=>{Object.defineProperty(navigator,'clipboard',{configurable:true,value:undefined});document.execCommand=()=>{throw Error('denied')}});await frame.locator('#copy').click();assert((await frame.locator('#status').innerText()).startsWith('Copy unavailable'))});
await check('Keyboard activation and navigation',async()=>{await frame.locator('#reset').focus();await page.keyboard.press('Enter');assert((await snap()).mode.endsWith('Word'));await frame.locator('#filter').focus();await page.keyboard.press('Space');assert(await frame.locator('#filter').isChecked());await frame.locator('#before').focus();await page.keyboard.press('Tab');assert.equal(await frame.evaluate(()=>document.activeElement.id),'after');await frame.locator('#compare').focus();await page.keyboard.press('Enter');assert.equal(await frame.evaluate(()=>document.activeElement.id),'compare')});
await check('Six widths and 200% root font at 320: no horizontal overflow with 6000-char unbroken content',async()=>{await compare('x'.repeat(6000),'y'.repeat(6000));for(const width of [320,375,768,1024,1440,1600]){await page.setViewportSize({width,height:1100});assert(await frame.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),`overflow at ${width}`)}await page.setViewportSize({width:320,height:1100});await frame.evaluate(()=>document.documentElement.style.fontSize='32px');assert(await frame.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),'overflow at 200%');await frame.evaluate(()=>document.documentElement.style.fontSize='')});
await check('Reduced motion has no animations',async()=>{await page.emulateMedia({reducedMotion:'reduce'});assert(await frame.evaluate(()=>matchMedia('(prefers-reduced-motion: reduce)').matches&&document.getAnimations().length===0))});
await check('Touch emulation presets and comparison',async()=>{const context=await browser.newContext({hasTouch:true,isMobile:true,viewport:{width:375,height:812}});const p=await context.newPage();p.on('pageerror',e=>errors.push(e.message));p.on('request',r=>requests.push(r.url()));await p.goto('http://127.0.0.1:5181');const f=p.frames().find(f=>f.url().endsWith('/revision-lens.html'));await f.locator('#packing').tap();assert((await f.locator('#report').inputValue()).includes('Unchanged: 3'));await f.locator('#clear').tap();assert.equal(await f.locator('#equality').innerText(),'Both texts are empty.');assert(await f.evaluate(()=>navigator.maxTouchPoints>0));await context.close()});
await check('All button/select/textarea targets at least 44px and labels at least 14px',async()=>{assert.deepEqual(await frame.locator('button,select,textarea').evaluateAll(es=>es.filter(e=>e.getBoundingClientRect().height<44||e.getBoundingClientRect().width<44).map(e=>e.id)),[]);assert.deepEqual(await frame.locator('label').evaluateAll(es=>es.filter(e=>parseFloat(getComputedStyle(e).fontSize)<14).map(e=>e.htmlFor)),[])});
await check('Zero external requests and no page script errors',async()=>{assert.deepEqual(requests.filter(u=>!u.startsWith('http://127.0.0.1:5181')),[]);assert.deepEqual(errors,[])});
}catch(e){results.push({name:'Harness',passed:false,error:String(e.stack||e)})}finally{if(browser)await browser.close();await new Promise(r=>server.close(r));const out={runTime:new Date().toISOString(),browser:'Installed Google Chrome, Playwright Chromium driver, headless',passed:results.filter(x=>x.passed).length,failed:results.filter(x=>!x.passed).length,results,requests,scriptErrors:errors,serverStopped:true};fs.writeFileSync(root+'/checks.json',JSON.stringify(out,null,2));console.log(JSON.stringify(out,null,2))}

import {createRequire} from 'node:module';import fs from 'node:fs';import assert from 'node:assert/strict';import {DatabaseSync} from 'node:sqlite';
const {chromium}=createRequire(import.meta.url)('/Users/witchaudio/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const root='/Users/witchaudio/.codex/.chatgpt-projects/g-p-6a9af954cc548191b3ccb0b0cf52f8a8/repo-reference',entry=JSON.parse(fs.readFileSync(root+'/lib/daily-project-content.json')).find(x=>x.slug==='chance-drawer');const base=process.env.CHANCE_HOST||'http://127.0.0.1:5178',headers=process.env.CHANCE_TOKEN?{'OAI-Sites-Authorization':'Bearer '+process.env.CHANCE_TOKEN}:{},live=base.startsWith('https');
const b=await chromium.launch(),context=await b.newContext({viewport:{width:1440,height:1100},acceptDownloads:true,extraHTTPHeaders:headers}),p=await context.newPage(),errors=[],checks=[];p.on('pageerror',e=>errors.push(e.message));const check=async(n,fn)=>{await fn();checks.push(n);console.log('PASS '+n)};
try{
await p.goto(base+'/');await check('Catalog retains all eight projects and adds Chance Drawer',async()=>{for(const slug of ['light-relay','orbit-notes','pixel-garden','signal-drift','studio-desk','tempo-lab','glyph-loom','curve-cabinet','chance-drawer'])await p.locator(`a[href^="/prompts/${slug}"]`).first().waitFor()});
await p.goto(base+'/prompts/chance-drawer');await p.waitForFunction(()=>[...document.querySelectorAll('button')].some(e=>e.textContent.includes('Try demo')&&Object.keys(e).some(k=>k.startsWith('__reactProps$'))));await p.getByRole('button',{name:'Try demo',exact:true}).click();await p.frameLocator('iframe[title="Chance Drawer interactive demo"]').locator('#exact').waitFor();
await check('Integrated readiness, sampling and host reset',async()=>{await p.waitForFunction(()=>!document.querySelector('.demo-state'));assert.equal(await p.locator('iframe[title="Chance Drawer interactive demo"]').getAttribute('sandbox'),'allow-scripts');let f=p.frames().find(x=>x.url().includes('/demos/chance-drawer'));assert.equal(await f.locator('#exact').textContent(),'30.00%');await f.getByRole('button',{name:'Replacement',exact:true}).click();assert.equal(await f.locator('#exact').textContent(),'28.80%');await f.locator('#draw').click();assert.equal(await f.locator('#trials').textContent(),'1');await p.getByRole('button',{name:'Reset',exact:true}).click();await p.waitForFunction(()=>!document.querySelector('.demo-state'));await p.frameLocator('iframe[title="Chance Drawer interactive demo"]').locator('#exact').waitFor();f=p.frames().find(x=>x.url().includes('/demos/chance-drawer'));assert.equal(await f.locator('#a').inputValue(),'4');assert.equal(await f.locator('#trials').textContent(),'0');assert.equal(await f.locator('#exact').textContent(),'30.00%')});
await check('Read-only Markdown includes complete ordered recipe',async()=>{await p.getByRole('tab',{name:'Markdown',exact:true}).click();const text=await p.locator('.recipe-markdown-source').textContent();for(const s of entry.recipe.steps)assert.ok(text.includes(s.text));assert.ok(text.includes(entry.recipe.setup));assert.ok(text.includes(entry.recipe.limits))});
await check('Six page widths and mobile demo controls',async()=>{for(const width of [320,375,768,1024,1440,1600]){await p.setViewportSize({width,height:1000});assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true)}await p.setViewportSize({width:375,height:1000});const f=p.frames().find(x=>x.url().includes('/demos/chance-drawer'));await f.getByRole('button',{name:'Replacement',exact:true}).click();await f.locator('#batch').click();assert.equal(await f.locator('#trials').textContent(),'100');assert.equal(await f.locator('#exact').textContent(),'28.80%');await f.locator('#reset').click();await p.evaluate(()=>scrollTo(0,0));await p.screenshot({path:'/tmp/chance-host-'+(live?'live':'local')+'-mobile.png',fullPage:false})});
const downloadPath='/api/recipes/chance-drawer/download?version=daily-chance-drawer-v1';
await check('Exact Markdown attachment, matching demo source and CSP',async()=>{const r=await context.request.get(base+downloadPath,{headers:{'User-Agent':'ChanceDrawerPreviewBot'}});assert.equal(r.status(),200);assert.match(r.headers()['content-disposition'],/chance-drawer\.md/);const text=await r.text();assert.ok(text.includes('Recipe version: daily-chance-drawer-v1'));for(const s of entry.recipe.steps)assert.ok(text.includes(s.text));assert.ok(text.includes(entry.recipe.limits));assert.ok(!text.includes('<!doctype html>'));fs.writeFileSync('/tmp/chance-'+(live?'live':'local')+'-recipe.md',text);const demo=await context.request.get(base+'/demos/chance-drawer');assert.match(demo.headers()['content-security-policy'],/sandbox allow-scripts/);assert.equal(await demo.text(),fs.readFileSync(root+'/demo-assets/chance-drawer.html','utf8'))});
if(!live){await check('Native browser recipe download completes',async()=>{const event=p.waitForEvent('download');await p.getByRole('button',{name:'Download pack',exact:true}).click();const d=await event;assert.equal(d.suggestedFilename(),'chance-drawer.md');assert.equal(await d.failure(),null);await d.saveAs('/tmp/chance-native-download.md');assert.equal(fs.readFileSync('/tmp/chance-native-download.md','utf8'),fs.readFileSync('/tmp/chance-local-recipe.md','utf8'))});
await check('Download analytics HEAD +0, GET +1, bot/error +0',async()=>{const dir=root+'/.wrangler/state/v3/d1/miniflare-D1DatabaseObject';const file=fs.readdirSync(dir).filter(x=>x.endsWith('.sqlite')&&x!=='metadata.sqlite').map(x=>dir+'/'+x).find(x=>{const d=new DatabaseSync(x,{readOnly:true});try{return !!d.prepare("SELECT name FROM sqlite_master WHERE name='recipe_download_counts'").get()}finally{d.close()}});const db=new DatabaseSync(file,{readOnly:true});const n=()=>db.prepare("SELECT coalesce(sum(downloads),0) n FROM recipe_download_counts WHERE prompt_id='daily-chance-drawer'").get().n;const a=n();await context.request.head(base+downloadPath);assert.equal(n(),a);await context.request.get(base+downloadPath,{headers:{'User-Agent':'ChanceDrawerHumanCheck'}});assert.equal(n(),a+1);await context.request.get(base+downloadPath,{headers:{'User-Agent':'PreviewBot'}});await context.request.get(base+'/api/recipes/chance-drawer/download?version=missing');assert.equal(n(),a+1);db.close()});}
await check('No page script errors',async()=>assert.deepEqual(errors,[]));fs.writeFileSync('/tmp/chance-host-'+(live?'live':'local')+'.json',JSON.stringify({at:new Date().toISOString(),base,checks,errors,nativeDownloadTested:!live},null,2));
}finally{await b.close()}

const fs=require('fs');const http=require('http');const assert=require('assert/strict');const {chromium}=require('/Users/witchaudio/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const root=__dirname;const evidence={started:new Date().toISOString(),tests:[],repairs:['Fixed an extra closing parenthesis in the distribution grid after inspecting computed CSS; added explicit grayscale accent color.','Applied clarified cap focus behavior: when focused draw control disables, focus Clear trials; verified Copy fallback focus restoration.','Expanded screenshot host iframe to document height to capture complete initial and sampled artifact.'],limits:['Chromium only; touch emulation is not physical-device testing.','Clipboard success/denial branches use injected browser APIs; native sandbox clipboard behavior also checked.','Independent agent reproduction from prompt files only; not independent-human testing or pixel comparison.']};
const wrapper=`<!doctype html><meta name="viewport" content="width=device-width,initial-scale=1"><style>html,body{margin:0;width:100%;height:100%}iframe{display:block;width:100%;height:100%;border:0}</style><iframe id="demo" sandbox="allow-scripts" src="/chance-drawer.html"></iframe><script>window.readyEvents=[];addEventListener('message',e=>{if(e.data?.type==='pls-demo-ready')readyEvents.push({origin:e.origin,data:e.data})})</script>`;
const server=http.createServer((req,res)=>{res.setHeader('Content-Type','text/html');res.end(req.url==='/chance-drawer.html'?fs.readFileSync(root+'/chance-drawer.html'):wrapper)});
(async()=>{await new Promise(r=>server.listen(5181,'127.0.0.1',r));const browser=await chromium.launch({headless:true});const context=await browser.newContext({viewport:{width:1440,height:1100}});const page=await context.newPage();const errors=[],external=[];page.on('pageerror',e=>errors.push(e.message));page.on('request',r=>{if(!r.url().startsWith('http://127.0.0.1:5181/'))external.push(r.url())});await page.goto('http://127.0.0.1:5181/');await page.waitForFunction(()=>readyEvents.length===1);const frame=page.frames()[1];
async function test(name,fn){try{let detail=await fn();evidence.tests.push({name,status:'pass',detail});}catch(e){evidence.tests.push({name,status:'fail',error:e.stack});throw e}}
async function screenshot(name){let height=await frame.evaluate(()=>document.documentElement.scrollHeight);await page.evaluate(h=>document.querySelector('iframe').style.height=h+'px',height);await page.screenshot({path:root+'/'+name,fullPage:true});await page.evaluate(()=>document.querySelector('iframe').style.height='100%');}
async function state(){return frame.evaluate(()=>({c:committed,dp:distribution,exact,trials,matches,history,report:$('report').value}))}
async function set(c){for(let key of ['a','b','n','target'])if(key in c)await frame.locator('#'+key).fill(String(c[key]));if('event'in c)await frame.locator('#event').selectOption(c.event);if('replacement'in c)await frame.locator('#replacement').setChecked(c.replacement);await frame.locator('#apply').click();}
try{
await test('Opaque sandbox, CSP, readiness, denied storage',async()=>{assert.equal(await page.evaluate(()=>readyEvents[0].origin),'null');assert.equal(await frame.evaluate(()=>document.querySelector('meta[http-equiv]').content),"default-src 'none'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; img-src data:; connect-src 'none'");assert.equal(await frame.evaluate(()=>{try{localStorage.setItem('check','1');return false}catch(e){return e.name==='SecurityError'}}),true);return 'ready origin null; localStorage SecurityError; exact CSP';});
await test('Balanced initial metrics and distribution',async()=>{let s=await state();[1/6,.5,.3,1/30].forEach((p,k)=>assert.ok(Math.abs(s.dp[k]-p)<1e-12));assert.equal(await frame.locator('#exact').innerText(),'30.00%');assert.equal(await frame.locator('#expected').innerText(),'1.20');assert.equal(await frame.locator('#bag .token').count(),10);assert.equal(await frame.locator('#observed').innerText(),'—');await screenshot('initial.png');return s.dp;});
await test('Exhaustive independent combinatorics comparison',async()=>{return frame.evaluate(()=>{function choose(n,k){if(k<0||k>n)return 0;let result=1;for(let i=1;i<=Math.min(k,n-k);i++)result=result*(n-i+1)/i;return result;}let cases=0,outcomes=0,operatorChecks=0,maxError=0;for(let a=0;a<=20;a++)for(let b=0;b<=20;b++){if(!a&&!b)continue;for(let replacement of [false,true])for(let n=1;n<=10;n++){if(!replacement&&n>a+b)continue;let c={a,b,n,replacement},dp=probabilities(c);let sum=dp.reduce((s,p)=>s+p,0);if(Math.abs(sum-1)>1e-12)throw Error('sum '+JSON.stringify(c));const refs=dp.map((p,k)=>replacement?choose(n,k)*(a/(a+b))**k*(b/(a+b))**(n-k):choose(a,k)*choose(b,n-k)/choose(a+b,n));dp.forEach((p,k)=>{let err=Math.abs(p-refs[k]);maxError=Math.max(maxError,err);if(err>1e-12||p<0)throw Error('formula '+JSON.stringify(c));outcomes++;});for(let target=0;target<=n;target++)for(let event of ['exact','least','most']){const actual=dp.reduce((s,p,k)=>s+(qualifies(k,{...c,target,event})?p:0),0);const expected=refs.reduce((s,p,k)=>s+((event==='exact'?k===target:event==='least'?k>=target:k<=target)?p:0),0);if(Math.abs(actual-expected)>1e-12)throw Error('operator');operatorChecks++;}cases++;}}return {cases,outcomes,operatorChecks,maxError};});});
await test('Hand calculations and preset immediate commits',async()=>{await set({a:2,b:4,n:3,target:2,event:'exact',replacement:false});let s=await state();[.2,.6,.2,0].forEach((p,k)=>assert.ok(Math.abs(p-s.dp[k])<1e-12));await set({a:4,b:6,n:3,target:2,event:'least'});assert.equal(await frame.locator('#exact').innerText(),'33.33%');await set({event:'most'});assert.equal(await frame.locator('#exact').innerText(),'96.67%');await frame.locator('#a').fill('19');await frame.locator('#rare').click();assert.equal(await frame.locator('#a').inputValue(),'1');assert.equal(await frame.locator('#exact').innerText(),'30.00%');await frame.locator('#replacement-preset').click();assert.equal(await frame.locator('#exact').innerText(),'28.80%');return 'Alternate distribution 20/60/20/0; Balanced least2 33.33%, most2 96.67%; Rare 30%; Replacement 28.80%';});
await test('Edit isolation; validation atomicity and preserved text/trials/report',async()=>{await frame.locator('#balanced').click();await frame.locator('#hundred').click();const before=await state();await frame.locator('#a').fill('12');assert.deepEqual(await state(),before);let cases=[{a:''},{a:'1.5'},{a:'21'},{a:'-1'},{b:'21'},{n:'0'},{n:'11'},{target:'-1'},{a:'0',b:'0'},{target:'4'},{a:'1',b:'1',n:'3'}];for(let bad of cases){await frame.evaluate(()=>{for(let k of ['a','b','n','target'])$(k).value=committed[k]});for(let [k,v]of Object.entries(bad))await frame.locator('#'+k).fill(v);let entered=await frame.evaluate(()=>Object.fromEntries(['a','b','n','target'].map(k=>[k,$(k).value])));await frame.locator('#apply').click();assert.deepEqual(await state(),before);assert.deepEqual(await frame.evaluate(()=>Object.fromEntries(['a','b','n','target'].map(k=>[k,$(k).value]))),entered);assert.ok(await frame.locator('[aria-invalid=true]').count()>0);assert.ok((await frame.locator('#status').innerText()).length>0);}await frame.evaluate(()=>{$('a').value='1e999'});await frame.locator('#apply').click();assert.deepEqual(await state(),before);return {rejections:cases.length+1,note:'number input sanitizes nonfinite assignment to blank; blank/nonfinite rejection path verified'};});
await test('Clear preserves pending fields; reset discards edits and errors',async()=>{await frame.locator('#a').fill('17');await frame.locator('#clear').click();assert.equal(await frame.locator('#a').inputValue(),'17');assert.equal((await state()).c.a,4);assert.equal((await state()).trials,0);await frame.locator('#reset').click();assert.equal(await frame.locator('#a').inputValue(),'4');assert.equal(await frame.locator('[aria-invalid=true]').count(),0);assert.equal((await state()).history.length,0);});
await test('Deterministic simulation, depletion, ordered actual history and exactly 100',async()=>{await frame.evaluate(()=>{window.originalRandom=Math.random;Math.random=()=>0});await frame.locator('#once').click();assert.deepEqual((await state()).history[0].picks,['A','A','A']);await frame.evaluate(()=>Math.random=()=>.999999);await frame.locator('#once').click();assert.deepEqual((await state()).history[0].picks,['B','B','B']);await set({a:1,b:2,n:3,target:1});await frame.evaluate(()=>Math.random=()=>0);await frame.locator('#once').click();assert.deepEqual((await state()).history[0].picks,['A','B','B']);await frame.locator('#balanced').click();await frame.evaluate(()=>{window.calls=0;window.seq=[0,.99,0,.99,.99,.99];Math.random=()=>seq[calls++%seq.length]});await frame.locator('#once').click();await frame.locator('#hundred').click();const s=await state();assert.equal(s.trials,101);assert.equal(s.matches,51);assert.equal(await frame.evaluate(()=>calls),303);assert.deepEqual(s.history.map(h=>h.number),[101,100,99,98,97,96,95,94]);assert.equal(s.history.length,8);for(let h of s.history)assert.deepEqual(h.picks,h.number%2?['A','B','A']:['B','B','B']);assert.equal(await frame.locator('#observed').innerText(),'50.50%');assert.equal(await frame.locator('#gap').innerText(),'+20.50 pp');await screenshot('sampled.png');await frame.evaluate(()=>Math.random=originalRandom);return {trials:s.trials,matches:s.matches,randomCalls:303,history:s.history};});
await test('Boundary experiments and zero-probability selected outcome labels',async()=>{for(let c of [{a:0,b:4,n:3,target:0,event:'exact',replacement:false},{a:4,b:0,n:3,target:3},{a:2,b:3,n:5,target:2},{a:1,b:0,n:10,target:10,replacement:true}]){await set(c);assert.equal(await frame.locator('#exact').innerText(),'100.00%');await frame.locator('#once').click();assert.equal((await state()).matches,1);}await set({a:0,b:1,n:10,target:10,event:'exact',replacement:true});assert.equal(await frame.locator('#exact').innerText(),'0.00%');assert.ok((await frame.locator('#distribution li').last().innerText()).includes('Selected event'));await frame.locator('#once').click();assert.equal(await frame.locator('#gap').innerText(),'0.00 pp');return 'zero A/B, whole bag, target 0/n, ten replacement picks from single token, selected impossible event';});
await test('Cap disables before exceeding 10000 and clear re-enables',async()=>{await frame.locator('#balanced').click();await frame.evaluate(()=>Math.random=()=>.5);for(let i=0;i<99;i++)await frame.locator('#hundred').click();await frame.locator('#once').click();assert.equal((await state()).trials,9901);assert.ok(await frame.locator('#hundred').isDisabled());assert.ok(await frame.locator('#once').isEnabled());for(let i=0;i<99;i++)await frame.locator('#once').click();assert.equal((await state()).trials,10000);assert.equal(await frame.evaluate(()=>document.activeElement.id),'clear');assert.ok(await frame.locator('#once').isDisabled());assert.ok(await frame.locator('#hundred').isDisabled());await frame.locator('#clear').click();assert.ok(await frame.locator('#once').isEnabled());assert.ok(await frame.locator('#hundred').isEnabled());await frame.evaluate(()=>Math.random=originalRandom);for(let i=0;i<99;i++)await frame.locator('#hundred').click();await frame.locator('#hundred').focus();await page.keyboard.press('Enter');assert.equal((await state()).trials,10000);assert.equal(await frame.evaluate(()=>document.activeElement.id),'clear');await frame.locator('#clear').click();return {cappedAt:10000,focus:'once and hundred transfer to Clear at cap'};});
await test('Rounded negative zero normalization',async()=>{await frame.evaluate(()=>{trials=10000;matches=3000;exact=.30000000000000004;renderTrials()});assert.equal(await frame.locator('#gap').innerText(),'0.00 pp');await frame.locator('#balanced').click();return 'Controlled display rounding edge; normal committed exact restored afterward';});
await test('Keyboard, button focus, full report selection and clipboard branches',async()=>{await frame.locator('#once').focus();await page.keyboard.press('Enter');assert.equal((await state()).trials,1);assert.equal(await frame.evaluate(()=>document.activeElement.id),'once');await page.keyboard.press('Space');assert.equal((await state()).trials,2);await frame.locator('#replacement').focus();let before=await frame.locator('#replacement').isChecked();await page.keyboard.press('Space');assert.equal(await frame.locator('#replacement').isChecked(),!before);await frame.locator('#apply').focus();await page.keyboard.press('Enter');assert.equal(await frame.evaluate(()=>document.activeElement.id),'apply');await frame.locator('#select').click();assert.ok(await frame.evaluate(()=>document.activeElement===$('report')&&$('report').selectionStart===0&&$('report').selectionEnd===$('report').value.length));await frame.locator('#copy').click();let native=await frame.locator('#status').innerText();assert.ok(native==='Copied'||native.startsWith('Copy unavailable.'));await frame.evaluate(()=>{Object.defineProperty(navigator,'clipboard',{configurable:true,value:{writeText:async text=>{window.copiedText=text}}})});await frame.locator('#copy').click();assert.equal(await frame.locator('#status').innerText(),'Copied');assert.equal(await frame.evaluate(()=>copiedText),await frame.locator('#report').inputValue());await frame.evaluate(()=>{Object.defineProperty(navigator,'clipboard',{configurable:true,value:{writeText:async()=>{throw Error('denied')}}});document.execCommand=()=>true});await frame.locator('#copy').click();assert.equal(await frame.locator('#status').innerText(),'Copied');assert.equal(await frame.evaluate(()=>document.activeElement.id),'copy');await frame.evaluate(()=>document.execCommand=()=>false);await frame.locator('#copy').click();assert.equal(await frame.locator('#status').innerText(),'Copy unavailable. Use Select report and your browser copy command.');await frame.evaluate(()=>{Object.defineProperty(navigator,'clipboard',{configurable:true,get(){throw Error('blocked')}});document.execCommand=()=>{throw Error('blocked')}});await frame.locator('#copy').click();assert.ok((await frame.locator('#status').innerText()).startsWith('Copy unavailable.'));return {nativeResult:native,branches:['injected async success','async denial + fallback success','both denied','guarded getters and execCommand throws']};});
await test('Responsive widths and 200% root text with no horizontal overflow',async()=>{let results=[];for(let width of [320,375,768,1024,1440,1600]){await page.setViewportSize({width,height:1000});let result=await frame.evaluate(()=>({width:innerWidth,scroll:document.documentElement.scrollWidth,body:document.body.scrollWidth,columns:getComputedStyle(document.querySelector('.workspace')).gridTemplateColumns,distributionColumns:getComputedStyle($('distribution')).gridTemplateColumns,smallestAction:Math.min(...Array.from(document.querySelectorAll('button')).map(e=>e.getBoundingClientRect().height)),smallestLabel:Math.min(...Array.from(document.querySelectorAll('label')).map(e=>parseFloat(getComputedStyle(e).fontSize)))}));assert.ok(result.scroll<=width&&result.body<=width,JSON.stringify(result));assert.ok(result.smallestAction>=44);assert.ok(result.smallestLabel>=14);if(width>=760){assert.equal(result.columns.split(' ').length,2);assert.ok(result.distributionColumns.split(' ').length>1);} else assert.equal(result.columns.split(' ').length,1);results.push(result);}await page.setViewportSize({width:320,height:1000});await frame.evaluate(()=>document.documentElement.style.fontSize='200%');assert.ok(await frame.evaluate(()=>document.documentElement.scrollWidth<=innerWidth&&document.body.scrollWidth<=innerWidth));await page.screenshot({path:root+'/text-scale-320.png',fullPage:true});await frame.evaluate(()=>document.documentElement.style.fontSize='');return results;});
await test('Touch emulation interactions',async()=>{const touch=await browser.newContext({viewport:{width:375,height:812},isMobile:true,hasTouch:true,deviceScaleFactor:2});const p=await touch.newPage();await p.goto('http://127.0.0.1:5181/');const f=p.frames()[1];await f.locator('#once').tap();assert.equal(await f.locator('#trials').innerText(),'1');await f.locator('#clear').tap();assert.equal(await f.locator('#trials').innerText(),'0');await touch.close();return '375px mobile/touch emulation Draw once/Clear trials';});
await test('No external requests or script errors',async()=>{assert.deepEqual(errors,[]);assert.deepEqual(external,[]);return {errors,external};});
}catch(e){evidence.failure=e.stack;process.exitCode=1;}finally{evidence.finished=new Date().toISOString();evidence.passed=evidence.tests.filter(t=>t.status==='pass').length;evidence.failed=evidence.tests.filter(t=>t.status==='fail').length;fs.writeFileSync(root+'/evidence.json',JSON.stringify(evidence,null,2));await browser.close();await new Promise(r=>server.close(r));console.log(JSON.stringify({passed:evidence.passed,failed:evidence.failed,failure:evidence.failure,tests:evidence.tests.map(t=>({name:t.name,status:t.status}))},null,2));}})();

import {createRequire} from 'node:module';import fs from 'node:fs';import assert from 'node:assert/strict';import {DatabaseSync} from 'node:sqlite';
const {chromium}=createRequire(import.meta.url)('/Users/witchaudio/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const root='/Users/witchaudio/.codex/.chatgpt-projects/g-p-6a9af954cc548191b3ccb0b0cf52f8a8/repo-reference',entry=JSON.parse(fs.readFileSync(root+'/lib/daily-project-content.json')).find(x=>x.slug==='curve-cabinet');const base=process.env.CURVE_HOST||'http://127.0.0.1:5178',headers=process.env.CURVE_TOKEN?{'OAI-Sites-Authorization':'Bearer '+process.env.CURVE_TOKEN}:{},live=base.startsWith('https');
const b=await chromium.launch(),context=await b.newContext({viewport:{width:1440,height:1100},acceptDownloads:true,extraHTTPHeaders:headers}),p=await context.newPage(),errors=[],checks=[];p.on('pageerror',e=>errors.push(e.message));const check=async(n,fn)=>{await fn();checks.push(n);console.log('PASS '+n)};
try{
await p.goto(base+'/');await check('Catalog retains all seven projects and adds Curve Cabinet',async()=>{for(const slug of ['light-relay','orbit-notes','pixel-garden','signal-drift','studio-desk','tempo-lab','glyph-loom','curve-cabinet'])await p.locator(`a[href^="/prompts/${slug}"]`).first().waitFor()});
await p.goto(base+'/prompts/curve-cabinet');await p.waitForFunction(()=>[...document.querySelectorAll('button')].some(e=>e.textContent.includes('Try demo')&&Object.keys(e).some(k=>k.startsWith('__reactProps$'))));await p.getByRole('button',{name:'Try demo',exact:true}).click();await p.frameLocator('iframe[title="Curve Cabinet interactive demo"]').locator('#plot').waitFor();
await check('Integrated readiness, curve editing and host reset',async()=>{await p.waitForFunction(()=>!document.querySelector('.demo-state'));assert.equal(await p.locator('iframe[title="Curve Cabinet interactive demo"]').getAttribute('sandbox'),'allow-scripts');let f=p.frames().find(x=>x.url().includes('/demos/curve-cabinet'));assert.equal(await f.locator('#eased-percent').textContent(),'80.2%');await f.getByRole('button',{name:'Linear',exact:true}).click();assert.equal(await f.locator('#eased-percent').textContent(),'50.0%');await f.locator('#handle1').focus();await f.locator('#handle1').press('Shift+ArrowRight');assert.equal(await f.locator('#x1').inputValue(),'0.1');await p.getByRole('button',{name:'Reset',exact:true}).click();await p.waitForFunction(()=>!document.querySelector('.demo-state'));await p.frameLocator('iframe[title="Curve Cabinet interactive demo"]').locator('#plot').waitFor();f=p.frames().find(x=>x.url().includes('/demos/curve-cabinet'));assert.equal(await f.locator('#x1').inputValue(),'0.25');assert.equal(await f.locator('#eased-percent').textContent(),'80.2%')});
await check('Read-only Markdown includes complete ordered recipe',async()=>{await p.getByRole('tab',{name:'Markdown',exact:true}).click();const text=await p.locator('.recipe-markdown-source').textContent();for(const s of entry.recipe.steps)assert.ok(text.includes(s.text));assert.ok(text.includes(entry.recipe.setup));assert.ok(text.includes(entry.recipe.limits))});
await check('Six page widths and mobile demo controls',async()=>{for(const width of [320,375,768,1024,1440,1600]){await p.setViewportSize({width,height:1000});assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true)}await p.setViewportSize({width:375,height:1000});const f=p.frames().find(x=>x.url().includes('/demos/curve-cabinet'));await f.getByRole('button',{name:'Linear',exact:true}).click();await f.locator('#timeline').evaluate(el=>{el.value=250;el.dispatchEvent(new Event('input',{bubbles:true}))});assert.equal(await f.locator('#eased-percent').textContent(),'25.0%');await f.locator('#reset').click();await p.evaluate(()=>scrollTo(0,0));await p.screenshot({path:'/tmp/curve-host-'+(live?'live':'local')+'-mobile.png',fullPage:false})});
const downloadPath='/api/recipes/curve-cabinet/download?version=daily-curve-cabinet-v1';
await check('Exact Markdown attachment, matching demo source and CSP',async()=>{const r=await context.request.get(base+downloadPath,{headers:{'User-Agent':'CurveCabinetPreviewBot'}});assert.equal(r.status(),200);assert.match(r.headers()['content-disposition'],/curve-cabinet\.md/);const text=await r.text();assert.ok(text.includes('Recipe version: daily-curve-cabinet-v1'));for(const s of entry.recipe.steps)assert.ok(text.includes(s.text));assert.ok(text.includes(entry.recipe.limits));assert.ok(!text.includes('<!doctype html>'));fs.writeFileSync('/tmp/curve-'+(live?'live':'local')+'-recipe.md',text);const demo=await context.request.get(base+'/demos/curve-cabinet');assert.match(demo.headers()['content-security-policy'],/sandbox allow-scripts/);assert.equal(await demo.text(),fs.readFileSync(root+'/demo-assets/curve-cabinet.html','utf8'))});
if(!live){await check('Native browser recipe download completes',async()=>{const event=p.waitForEvent('download');await p.getByRole('button',{name:'Download pack',exact:true}).click();const d=await event;assert.equal(d.suggestedFilename(),'curve-cabinet.md');assert.equal(await d.failure(),null);await d.saveAs('/tmp/curve-native-download.md');assert.equal(fs.readFileSync('/tmp/curve-native-download.md','utf8'),fs.readFileSync('/tmp/curve-local-recipe.md','utf8'))});
await check('Download analytics HEAD +0, GET +1, bot/error +0',async()=>{const dir=root+'/.wrangler/state/v3/d1/miniflare-D1DatabaseObject';const file=fs.readdirSync(dir).filter(x=>x.endsWith('.sqlite')&&x!=='metadata.sqlite').map(x=>dir+'/'+x).find(x=>{const d=new DatabaseSync(x,{readOnly:true});try{return !!d.prepare("SELECT name FROM sqlite_master WHERE name='recipe_download_counts'").get()}finally{d.close()}});const db=new DatabaseSync(file,{readOnly:true});const n=()=>db.prepare("SELECT coalesce(sum(downloads),0) n FROM recipe_download_counts WHERE prompt_id='daily-curve-cabinet'").get().n;const a=n();await context.request.head(base+downloadPath);assert.equal(n(),a);await context.request.get(base+downloadPath,{headers:{'User-Agent':'CurveCabinetHumanCheck'}});assert.equal(n(),a+1);await context.request.get(base+downloadPath,{headers:{'User-Agent':'PreviewBot'}});await context.request.get(base+'/api/recipes/curve-cabinet/download?version=missing');assert.equal(n(),a+1);db.close()});}
await check('No page script errors',async()=>assert.deepEqual(errors,[]));fs.writeFileSync('/tmp/curve-host-'+(live?'live':'local')+'.json',JSON.stringify({at:new Date().toISOString(),base,checks,errors,nativeDownloadTested:!live},null,2));
}finally{await b.close()}

const {chromium}=require('/Users/witchaudio/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const fs=require('fs');const path=__dirname;const assert=require('node:assert/strict');const source=fs.readFileSync(path+'/curve-cabinet.html','utf8');const report={model:'GPT-6 Astra, High (inherited parent configuration)',artifact:'Independent empty-directory reproduction from /tmp/curve-recipe.json only',checks:[],repairs:['Browser math check repaired negative-zero lead display by rounding before choosing sign.','Code inspection before browser run: capture timeline input before pause() rerenders its value.'],limitations:['Chromium only; touch uses CDP emulation rather than physical hardware.','Clipboard tests explicitly inject API results; real clipboard permission was not tested.','Hidden-document handling uses injected document.hidden and visibilitychange in a real browser; actual operating-system backgrounding was not tested.','NaN-like number input is browser-normalized to blank, and rejection is verified after that normalization.','No physical pen device or screen-reader test was performed.']};let browser;
async function run(name,fn){await fn();report.checks.push(name);console.log('PASS '+name)}
(async()=>{browser=await chromium.launch({headless:true});const context=await browser.newContext({viewport:{width:1440,height:1100}});const page=await context.newPage();let errors=[],requests=[];page.on('pageerror',e=>errors.push(String(e)));page.on('request',r=>requests.push(r.url()));let f;
async function load(options={}){await page.emulateMedia({reducedMotion:options.reduced?'reduce':'no-preference'});const inject=`<script>window.__raf={pending:0,max:0};const request=window.requestAnimationFrame.bind(window),cancel=window.cancelAnimationFrame.bind(window);const handles=new Set();window.requestAnimationFrame=cb=>{let id=request(t=>{handles.delete(id);window.__raf.pending=handles.size;cb(t)});handles.add(id);window.__raf.pending=handles.size;window.__raf.max=Math.max(window.__raf.max,handles.size);return id};window.cancelAnimationFrame=id=>{handles.delete(id);window.__raf.pending=handles.size;cancel(id)};<\/script>`;await page.setContent('<script>window.ready=[];addEventListener("message",e=>{if(e.data.type==="pls-demo-ready")window.ready.push(e.origin)})<\/script><iframe title="cabinet" sandbox="allow-scripts" style="border:0;width:100%;height:2100px"></iframe><style>html,body{margin:0}</style>');await page.locator('iframe').evaluate((el,html)=>el.srcdoc=html,source.replace('<script>',inject+'<script>'));await page.waitForFunction(()=>window.ready.length===1);f=page.frames()[1];await f.locator('#css').waitFor()}
const val=id=>f.locator('#'+id).inputValue();const txt=id=>f.locator('#'+id).textContent();const click=id=>f.locator('#'+id).click();async function scrub(u){await f.locator('#timeline').evaluate((el,v)=>{el.value=v;el.dispatchEvent(new Event('input',{bubbles:true}))},u*1000)}
function bez(u,p){if(u===0||u===1)return u;const B=(t,a,b)=>3*(1-t)*(1-t)*t*a+3*(1-t)*t*t*b+t*t*t;let low=0,high=1;for(let i=0;i<60;i++){const t=(low+high)/2;if(B(t,p[0],p[2])<u)low=t;else high=t}return B((low+high)/2,p[1],p[3])}
async function checkMath(p,u){await scrub(u);const e=bez(u,p);const v=await f.evaluate(()=>({x:+document.querySelector('#sample').getAttribute('cx'),y:+document.querySelector('#sample').getAttribute('cy'),your:parseFloat(document.querySelector('#yourMarker').style.left),linear:parseFloat(document.querySelector('#linearMarker').style.left)}));assert(Math.abs(v.x-u*100)<1e-7);assert(Math.abs(v.y-(1-e)*100)<1e-7);assert(Math.abs(v.your-e*100)<.0001);assert(Math.abs(v.linear-u*100)<1e-7);assert.equal(await txt('easedText'),(e*100).toFixed(1)+'%');const lead=Math.round((e-u)*1000)/10;assert.equal(await txt('leadText'),(lead>=0?'+':'')+lead.toFixed(1)+' pp')}
async function apply(p,d){for(const [i,id]of ['x1','y1','x2','y2'].entries())await f.locator('#'+id).fill(String(p[i]));await f.locator('#duration').fill(String(d));await click('apply')}
await load();await run('Opaque sandbox readiness, initial state, canonical CSS',async()=>{assert.deepEqual(await page.evaluate(()=>window.ready),['null']);assert.equal(await val('css'),'transition-duration: 1200ms;\ntransition-timing-function: cubic-bezier(0.25, 0.1, 0.25, 1);');assert.equal(await txt('state'),'Paused');assert.equal(await val('timeline'),'500');await page.screenshot({path:path+'/initial.png',fullPage:true});await checkMath([.25,.1,.25,1],.5);assert.equal(await txt('easedText'),'80.2%')});
await run('Four exact presets at 0%,25%,50%,75%,100%; sample, markers, lead, pressed state and CSS',async()=>{for(const[n,p]of Object.entries({Ease:[.25,.1,.25,1],Linear:[0,0,1,1],Glide:[.16,1,.3,1],'S-curve':[.65,0,.35,1]})){await f.locator(`[data-preset="${n}"]`).click();assert.equal(await txt('presetName'),n);assert.equal(await f.locator('[aria-pressed="true"]').count(),1);assert((await val('css')).includes(p.join(', ')));for(const u of [0,.25,.5,.75,1])await checkMath(p,u)}});
await run('Custom crossing coordinates and valid rounded atomic apply',async()=>{await apply([.9,.234,.1,.826],700);assert.equal(await txt('presetName'),'Custom');assert.equal(await val('y1'),'0.23');assert.equal(await val('y2'),'0.83');for(const u of [0,.25,.5,.75,1])await checkMath([.9,.23,.1,.83],u);assert.equal(await val('css'),'transition-duration: 700ms;\ntransition-timing-function: cubic-bezier(0.9, 0.23, 0.1, 0.83);');await scrub(.5);await page.screenshot({path:path+'/changed-curve.png',fullPage:true})});
await run('Invalid blank, NaN-like, range and duration inputs retain committed math/export and text',async()=>{for(const[id,input]of [['x1',''],['y1','NaN'],['x2','-0.1'],['y2','1.1'],['duration','250'],['duration','199'],['duration','3100']]){await apply([.9,.23,.1,.83],700);const css=await val('css'),sample=await f.locator('#sample').getAttribute('cy');await f.locator('#'+id).evaluate((el,v)=>el.value=v,input);const entered=await val(id);await click('apply');assert.equal(await val(id),entered);assert.equal(await val('css'),css);assert.equal(await f.locator('#sample').getAttribute('cy'),sample);assert.equal(await f.locator('#'+id).getAttribute('aria-invalid'),'true');assert.equal(await txt('presetName'),'Custom');assert((await txt('status')).includes('Correct'))}});
await run('Pending edits leave animation and committed output alone; preset/reset replace fields',async()=>{await click('reset');await click('play');await f.locator('#x1').fill('.99');assert.equal(await txt('state'),'Playing');assert((await val('css')).includes('0.25, 0.1, 0.25, 1'));await f.locator('[data-preset="Glide"]').click();assert.equal(await val('x1'),'0.16');assert.equal(await txt('state'),'Paused');await click('reset');assert.equal(await val('x1'),'0.25');assert.equal(await val('timeline'),'500')});
await run('Keyboard .01/Shift .1 increments, clamps, focus, unrelated key default',async()=>{await f.locator('#p1').focus();await f.locator('#p1').press('ArrowRight');assert.equal(await val('x1'),'0.26');await f.locator('#p1').press('Shift+ArrowUp');assert.equal(await val('y1'),'0.2');await apply([0,0,1,1],1200);await f.locator('#p1').focus();await f.locator('#p1').press('ArrowLeft');await f.locator('#p1').press('ArrowDown');assert.equal(await val('x1'),'0');assert.equal(await val('y1'),'0');assert(await f.locator('#p1').evaluate(el=>el===document.activeElement));assert(await f.locator('#p1').evaluate(el=>el.dispatchEvent(new KeyboardEvent('keydown',{key:'a',cancelable:true,bubbles:true}))));await f.locator('#p2').press('Shift+ArrowRight');assert.equal(await val('x2'),'1')});
await run('Real mouse drag rounds and clamps with pointer capture and release',async()=>{await click('reset');const box=await f.locator('#plot').boundingBox(),handle=await f.locator('#p1').boundingBox();await page.mouse.move(handle.x+22,handle.y+22);await page.mouse.down();await page.mouse.move(box.x+box.width*.333,box.y+box.height*.666);assert.equal(await val('x1'),'0.33');assert.equal(await val('y1'),'0.33');await page.mouse.move(box.x-20,box.y+box.height+20);assert.equal(await val('x1'),'0');assert.equal(await val('y1'),'0');await page.mouse.up();await page.mouse.move(box.x+box.width,box.y);assert.equal(await val('x1'),'0')});
await run('Secondary mouse button, secondary pointer and different-pointer move ignored; cancel/lost capture terminate',async()=>{await click('reset');const b=await f.locator('#p1').boundingBox();await page.mouse.move(b.x+22,b.y+22);await page.mouse.down({button:'right'});await page.mouse.move(b.x+100,b.y);await page.mouse.up({button:'right'});assert.equal(await val('x1'),'0.25');await f.locator('#p1').dispatchEvent('pointerdown',{pointerId:88,isPrimary:false,button:0,clientX:0,clientY:0});assert.equal(await val('x1'),'0.25');for(const type of ['pointercancel','lostpointercapture']){const bb=await f.locator('#p1').boundingBox();await page.mouse.move(bb.x+22,bb.y+22);await page.mouse.down();const before=await val('x1');await f.locator('#p1').dispatchEvent('pointermove',{pointerId:88,isPrimary:false,clientX:0,clientY:0});assert.equal(await val('x1'),before);await f.locator('#p1').dispatchEvent(type,{pointerId:1});await page.mouse.move(bb.x+90,bb.y);assert.equal(await val('x1'),before);await page.mouse.up()}});
await run('Play/pause/resume/replay/completion, single RAF, curve/scrub/reset cancellation',async()=>{await apply([.25,.1,.25,1],400);await scrub(.25);await click('play');await page.waitForTimeout(60);await click('play');let stopped=await val('timeline');await page.waitForTimeout(70);assert.equal(await val('timeline'),stopped);await click('play');await page.waitForTimeout(500);assert.equal(await txt('state'),'Complete');assert.equal(await val('timeline'),'1000');await click('play');assert.equal(await txt('state'),'Playing');await click('replay');await page.waitForTimeout(30);assert(+await val('timeline')<500);await f.locator('[data-preset="Linear"]').click();assert.equal(await txt('state'),'Paused');await click('play');await scrub(.325);assert.equal(await val('timeline'),'325');assert.equal(await txt('state'),'Paused');await click('play');await f.locator('#duration').fill('900');await click('apply');assert.equal(await txt('state'),'Paused');await click('reset');assert.equal(await val('timeline'),'500');assert.equal(await val('duration'),'1200');assert.equal(await txt('presetName'),'Ease');assert.deepEqual(await f.evaluate(()=>window.__raf),{pending:0,max:1})});
await run('Hidden-document branch pauses and returning visible remains paused (injected visibility)',async()=>{await click('play');await f.evaluate(()=>{Object.defineProperty(document,'hidden',{configurable:true,value:true});document.dispatchEvent(new Event('visibilitychange'))});assert.equal(await txt('state'),'Paused');await f.evaluate(()=>{Object.defineProperty(document,'hidden',{configurable:true,value:false});document.dispatchEvent(new Event('visibilitychange'))});await page.waitForTimeout(40);assert.equal(await txt('state'),'Paused')});
await run('Reduced-motion dynamic and initial disable play/replay while permitting scrub and reset',async()=>{await click('play');await page.emulateMedia({reducedMotion:'reduce'});await page.waitForTimeout(50);assert(await f.locator('#play').isDisabled());assert(await f.locator('#replay').isDisabled());assert.equal(await txt('state'),'Paused');await scrub(.2);assert.equal(await val('timeline'),'200');await page.emulateMedia({reducedMotion:'no-preference'});await page.waitForTimeout(50);assert.equal(await txt('state'),'Paused');await load({reduced:true});assert(await f.locator('#play').isDisabled());assert.equal(await txt('state'),'Paused');await scrub(.7);assert.equal(await val('timeline'),'700');await click('reset');assert.equal(await val('timeline'),'500')});
await run('Injected clipboard API success, rejection fallback success, false/throw failure, selection',async()=>{await f.evaluate(()=>{Object.defineProperty(navigator,'clipboard',{configurable:true,value:{writeText:async s=>{window.copied=s}}})});await click('copy');assert.equal(await txt('status'),'CSS copied.');assert.equal(await f.evaluate(()=>window.copied),await val('css'));await f.evaluate(()=>{Object.defineProperty(navigator,'clipboard',{configurable:true,value:{writeText:async()=>{throw Error('denied')}}});document.execCommand=()=>true});await click('copy');assert.equal(await txt('status'),'CSS copied.');for(const branch of ['false','throw']){await f.evaluate(v=>{document.execCommand=()=>{if(v==='throw')throw Error('denied');return false}},branch);await click('copy');assert((await txt('status')).startsWith('Copy unavailable.'))}await f.evaluate(()=>Object.defineProperty(navigator,'clipboard',{configurable:true,get(){throw Error('unavailable')}}));await click('copy');assert((await txt('status')).startsWith('Copy unavailable.'));await click('select');assert(await f.locator('#css').evaluate(el=>el===document.activeElement&&el.selectionStart===0&&el.selectionEnd===el.value.length))});
await run('320,375,768,1024,1440,1600 widths and 200% root text have no horizontal overflow; fields/handles stay inside viewport',async()=>{for(const width of [320,375,768,1024,1440,1600]){await page.setViewportSize({width,height:1100});for(const font of ['16px','32px']){await f.evaluate(size=>document.documentElement.style.fontSize=size,font);await apply([0,0,1,1],1200);const metrics=await f.evaluate(()=>({viewport:innerWidth,scroll:document.documentElement.scrollWidth,bounds:[...document.querySelectorAll('input[type=number],.handle')].map(el=>{let b=el.getBoundingClientRect();return{x:b.x,right:b.right,w:b.width}})}));assert(metrics.scroll<=metrics.viewport,JSON.stringify({width,font,metrics}));assert(metrics.bounds.every(b=>b.x>=0&&b.right<=metrics.viewport&&b.w>=44),JSON.stringify({width,font,metrics}));for(const u of [0,1]){await scrub(u);assert(await f.evaluate(()=>[...document.querySelectorAll('.marker')].every(el=>{const marker=el.getBoundingClientRect(),track=el.parentElement.parentElement.getBoundingClientRect();return marker.left>=track.left&&marker.right<=track.right})));}if(width===320&&font==='32px')await page.screenshot({path:path+'/320px-200percent.png',fullPage:true})}}});
await run('Touch emulation uses real CDP touch events with capture and same bounds',async()=>{await page.setViewportSize({width:375,height:1000});await f.evaluate(()=>document.documentElement.style.fontSize='16px');await click('reset');await f.locator('#p1').scrollIntoViewIfNeeded();const cdp=await context.newCDPSession(page);await cdp.send('Emulation.setTouchEmulationEnabled',{enabled:true,maxTouchPoints:5});const h=await f.locator('#p1').boundingBox(),p=await f.locator('#plot').boundingBox();await cdp.send('Input.dispatchTouchEvent',{type:'touchStart',touchPoints:[{x:h.x+22,y:h.y+22,id:0}]});await cdp.send('Input.dispatchTouchEvent',{type:'touchMove',touchPoints:[{x:p.x+p.width*.77,y:p.y+p.height*.22,id:0}]});assert.equal(await val('x1'),'0.77');assert.equal(await val('y1'),'0.78');await cdp.send('Input.dispatchTouchEvent',{type:'touchEnd',touchPoints:[]});await cdp.detach()});
await run('No script errors and zero network requests across browser suite',async()=>{assert.deepEqual(errors,[]);assert.deepEqual(requests,[])});report.status='passed';report.runTime=new Date().toISOString();fs.writeFileSync(path+'/results.json',JSON.stringify(report,null,2));await browser.close()})().catch(async error=>{report.status='failed';report.error=String(error.stack||error);report.runTime=new Date().toISOString();fs.writeFileSync(path+'/results.json',JSON.stringify(report,null,2));console.error(error);if(browser)await browser.close();process.exitCode=1});

# Curve Cabinet — September 8, 2026

Original and independent reproduction use GPT-6 Astra High. Full 16-concept ledger, all 16 live prompt rows and seven live gallery links checked before selection. Standalone cubic-Bezier easing tool is distinct from prior drawing, optical puzzle, audio sequencing, runner, garden, notes and workflow concepts.

## Verification

Original 13 Chromium groups passed; independent fresh-context reproduction 15 groups passed. Specification review confirmed repaired output live regions, rounded-zero lead and amber baseline. Code-quality review approved; supplemental Chromium probes checked atomic inputs, pointer cleanup, single RAF cancellation, clipboard fallbacks and sandbox isolation. Original screenshots captured directly from running demo, including desktop and changed Glide previews; mobile/200% screenshots inspected. Reproduction received only exact build and verification prompts; retained source is evidence, never part of recipe downloads. No packages, credentials, auth/access, schema, analytics or library changes.

Original repairs: animated `<output>` defaults were implicit polite live regions, so disabled with aria-live=off; tiny numerical zero normalized before sign; baseline marker changed to specified amber. Independent rebuild repairs: capture timeline value before pause rerenders, and normalize lead zero. These are Step 2 verification repairs, with no recipe-spec change. Pack is consolidated, not a verbatim transcript or one-shot claim.

Limits: Chromium only, CDP touch emulation, clipboard APIs and hidden-document state injected. No real OS backgrounding, actual clipboard permission test, physical-device, Safari or screen-reader study. Accessibility tree inspected for the output live-region issue; not equivalent to a screen-reader study.

## Rerun

From repository root copy demo-assets/curve-cabinet.html to /tmp/curve-cabinet.html; run node docs/daily-project-evidence/2026-09-08/server-original.mjs then node docs/daily-project-evidence/2026-09-08/verify-original.mjs. Server listens on 127.0.0.1:5177 and wraps demo in an opaque-origin iframe. Harness writes /tmp/curve-* captures/results. Playwright path is this machine's installed runtime; adapt it when moved.

Independent reproduction: copy reproduction.html as curve-cabinet.html and verify-reproduction.cjs.txt as verify.cjs into an empty folder, then run node verify.cjs from that folder. Harness reads the local file and exercises an opaque iframe. Preserve this evidence outside production recipe downloads.

Production build, typecheck, lint and all 72 repository tests passed. Eight local production-Worker host groups passed: all eight gallery projects, readiness/edit/reset, complete read-only recipe, six widths/mobile interaction, matching source/CSP and attachment, completed native browser file download, analytics HEAD +0 / ordinary GET +1 / bot and error +0, no page errors. Previous two daily recipe payloads structurally unchanged.

Host rerun: start node_modules/.bin/wrangler dev --config dist/server/wrangler.json --port 5178 --ip 127.0.0.1 --persist-to .wrangler/state, then run node docs/daily-project-evidence/2026-09-08/verify-host.mjs. Local database reads only verify aggregate counts. Live mode uses CURVE_HOST and existing authorized CURVE_TOKEN in process environment; never persist credentials. Live skips native file save and local SQLite counts and checks authenticated attachment bytes. No production private-save claim.

Background automation skips opening a user-facing browser tab. Final read-only integration review approved: schema/publication contract, build hash, demo mapping, real preview files, ordered recipe-only export and unchanged previous payloads checked. Deployment/live checks pending.
